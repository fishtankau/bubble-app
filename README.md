# Bubble App — SEATAC Airport Analytics Portal

A white-label, **static** embedded-analytics portal (React + Vite) for the
SEATAC Airport demo brand. It lands on a branded login and, once you sign in,
shows the dashboard output (Overview, AI Chat, Search, Flights, Hub) powered by
embedded Omni Analytics.

**Live site:** https://fishtankau.github.io/bubble-app/

## How it works

### Documentation to start

You can reference this embed portal repo to get something working really
quickly — with just your **Omni API key** and **embed secret** you can stand up
the same experience you see here: **AI Chat**, **dashboard embed**, and
**application embed**. Or use the Claude skills to spin up a branded portal of
your own.

- This repo *is* a runnable example — clone it, drop in your keys (see
  `src/context/BrandContext.jsx`), and you have a working embed portal.
- [Launch AI-powered embedded analytics →](https://omni.co/embedded-analytics)
- [Omni embed setup docs →](https://docs.omni.co/embed/setup/index)

### How embedding works

- Omni is embedded into an **`<iframe>`** inside your app (staging environment or
  localhost to start).
- For the iframe's `src`, you **dynamically generate a signed URL server-side**.
- The URL is signed using your **embed secret** (generated by you), then passed
  to the browser.
- For testing, you can leverage the [embed URL builder / test URLs](https://docs.omni.co/embed/setup/test-urls).
- In production, generate the URL with the [TypeScript SDK](https://docs.omni.co/embed/setup/standard-sso#sdk-recommended).
- See the full list of [parameters you can pass in the signed URL](https://docs.omni.co/embed/setup/url-parameters).
- Build **interactive experiences** by [leveraging JavaScript events](https://docs.omni.co/embed/events) (e.g. clicking a value in a dashboard to open a custom modal — this repo does exactly that in the Flights tab).

> **Note on this repo:** because it's hosted on static GitHub Pages (no server),
> it signs the embed URL **client-side** with the Web Crypto API and **demo-only**
> credentials. That's the one exception to the "sign server-side" rule above, and
> it's only safe because these are throwaway trial keys. In production, always
> sign server-side so your embed secret never reaches the browser.

### Embedding security — why it can't be tampered with

Omni embedding is **secure by default**. The key idea: the **embed secret is used
to *sign* the URL, not to *identify* the user.**

- **Identity** comes from the parameters you put in the signed payload —
  primarily **`externalId`** (your internal user ID), and optionally `name` and
  `email`. Omni uses `externalId` as the unique key to **create or match an embed
  user** on each session.
- **The signature** is an **HMAC-SHA256** over the parameters, computed with your
  embed secret. It simply proves the URL was generated by someone who holds the
  secret — it doesn't grant identity, it prevents forgery.

**Tamper-prevention is automatic.** Even with the standard (1-step) SSO call, the
URL is signed server-side with your embed secret (HMAC-SHA256 over the
parameters). If anyone edits `group_level_id`, the `entity`, a user attribute, or
*any* signed parameter in the browser, the signature no longer matches — and
**Omni rejects the session.** The URL simply can't be tampered with.

Here, **`group_level_id` is just an example of an _access-filter user attribute_** —
a value you pass in the signed payload that Omni uses for **role-based access
control (RBAC)**. An [access filter](https://docs.omni.co/modeling/topics/parameters/access-filters)
maps that [user attribute](https://docs.omni.co/administration/users/attributes)
to a field and injects it into the `WHERE` clause of every query, so each embedded
user only sees the **rows their attribute permits** (e.g. `group_level_id`,
`region`, or `brand_name` scoping data to one team, geography, or tenant). This is
exactly *why* the signature matters: if a user could edit their `group_level_id`
in the browser, they could grant themselves access to other rows. Because it's
part of the HMAC-signed payload, they can't — any change breaks the signature and
the session is rejected. (You pass these via the
[`userAttributes` parameter](https://docs.omni.co/embed/setup/url-parameters/userAttributes);
for sensitive attributes, use two-step SSO so they never appear in the URL.)

For higher-security flows where you don't want any parameters visible in the URL
at all, use **two-step SSO**, which exchanges the signed request for a short-lived
token before loading the iframe.

- [Standard SSO setup →](https://docs.omni.co/embed/setup/standard-sso)
- [Two-step SSO →](https://docs.omni.co/embed/setup/two-step-sso)
- [`externalId` & URL parameters →](https://docs.omni.co/embed/setup/url-parameters)
- [Access filters (RBAC) →](https://docs.omni.co/modeling/topics/parameters/access-filters)
- [User attributes →](https://docs.omni.co/administration/users/attributes)
- [Data access control guide →](https://docs.omni.co/modeling/develop/data-access-control)

### Embed case studies

- **WorkRamp** relaunched a faster, more customizable in-app data product in under
  three months — cutting support volume and saving ~2 engineers/quarter.
  [Read →](https://omni.co/blog/case-study-workramp)
- **BambooHR** launched its **Elite Analytics** tier to 30,000+ people in four
  months, defining metrics once in Omni's semantic layer and reusing them across
  customers. [Read →](https://omni.co/blog/case-study-bamboohr)
- **Brevo** built its AI analytics foundation on Omni, consolidating five BI tools
  into one platform for internal and customer-facing insights.
  [Read →](https://omni.co/blog/case-study-brevo)

[Browse all Omni customer case studies →](https://omni.co/customer-case-studies)
